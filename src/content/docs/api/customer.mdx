---
title: Customer endpoints
description: HTTP API for authenticated customer-facing actions on EV cards, sessions, and account state.
sidebar:
  order: 2
persona: Developer
surface: n/a
tier: P0
code_refs:
  - web/routes/api/customer/
last_authored: 2025-01-15
prompt_template: api-reference
---

import Glossary from "@components/Glossary.astro";

<!-- TODO: handler source files for web/routes/api/customer/ were not provided in the code references. The endpoint list below cannot be authored without walking the actual route handlers to determine path params, request shape, response shape, and error cases. -->

The `/api/customer/*` namespace exposes customer-facing actions: managing [<Glossary term="<Glossary term="EV card">EV card</Glossary>">EV cards</Glossary>](/glossary#ev-card), starting and stopping charging sessions, viewing history, and updating account settings. All endpoints require an authenticated customer session.

## Base URL

```
https://app.polaris.express
```

The customer API is served from the customer host, not the admin host. Requests to admin hosts will be rejected.

## Authentication

Every request must include a valid [<Glossary term="<Glossary term="BetterAuth">BetterAuth</Glossary>">BetterAuth</Glossary>](/glossary#betterauth) session cookie. Unauthenticated requests return `401`.

```http
Cookie: better-auth.session_token=<token>
```

Session tokens are issued by the auth flow (email/password or [magic link](/glossary#<Glossary term="<Glossary term="Magic link">Magic link</Glossary>">magic-link</Glossary>)) and are scoped to a single customer identity. The session subject determines which [EV cards](/glossary#ev-card), sessions, and account data are visible — customers cannot read or mutate resources they do not own.

## Endpoints

<!-- TODO: handler not found — endpoint reference cannot be generated without the route source files under web/routes/api/customer/. Re-run with code_refs populated to enumerate handlers. -->

## Examples

<!-- TODO: examples depend on enumerated endpoints; pending handler walk. -->